# Cargo workspace for the direct escrow program.
[workspace]
members = ["programs/escrow"]
resolver = "2"

# Anchor requires overflow checks on release builds (the security checklist) - set at the workspace root.
[profile.release]
overflow-checks = true
