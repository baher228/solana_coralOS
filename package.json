{
  "name": "solana-coralos",
  "version": "0.1.0",
  "private": true,
  "description": "Freelance escrow agent demo on the Solana CoralOS backbone. Run it with `npm run dev`.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "node scripts/txodds.js",
    "agent:demo-worker": "npm --prefix agents/demo-worker run dev",
    "agent:demo-worker:test": "npm --prefix agents/demo-worker run self-test"
  }
}
