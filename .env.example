# Copy to .env or run `node scripts/setup.js`. Never commit .env.

BUYER_KEYPAIR_B58=
SELLER_KEYPAIR_B58=
WALLET=
SOLANA_RPC_URL=https://api.devnet.solana.com

# Optional review brain. Without this, the demo uses deterministic local review.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
LLM_PROVIDER=
LLM_MODEL=
